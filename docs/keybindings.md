# Commands & keybindings

Generated by `ft commands docs`. Re-run after modifying any `CommandDef` slice. CI's `ft commands docs --check` verifies this file stays in sync.

## global

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `app.git-leader` | Enter the git-sync leader (then `s` to sync) | App | no |
| `app.help` | Open the keymap help overlay | App | no |
| `app.next-tab` | Switch to the next tab | Tabs | no |
| `app.prev-tab` | Switch to the previous tab | Tabs | no |
| `app.quit` | Quit the TUI | App | no |
| `app.switch-tab` | Switch to the tab at the given 0-based index | Tabs | no |

## tab/graph

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `graph.add-view` | Add a new view (pick preset or blank) | Views | yes |
| `graph.append` | Append a template to the selected note | Notes | yes |
| `graph.clear-multi-select` | Clear the multi-selection (Esc) | Notes | no |
| `graph.close-view` | Close the active view | Views | no |
| `graph.collapse-or-jump-parent` | Collapse the selected node (or jump to parent) | Navigation | no |
| `graph.create-blank` | Create a new note (blank) in the selected folder | Notes | yes |
| `graph.create-from-template` | Create a new note from a template | Notes | yes |
| `graph.cursor-down` | Move the cursor down one row | Navigation | no |
| `graph.cursor-first` | Jump to the first row | Navigation | no |
| `graph.cursor-half-page-down` | Move the cursor down half a page | Navigation | no |
| `graph.cursor-half-page-up` | Move the cursor up half a page | Navigation | no |
| `graph.cursor-last` | Jump to the last row | Navigation | no |
| `graph.cursor-up` | Move the cursor up one row | Navigation | no |
| `graph.expand-or-collapse` | Expand the selected node (or collapse if already expanded) | Navigation | no |
| `graph.journal` | Open the Journal tab for the selected note | Notes | no |
| `graph.move` | Enter the move-section flow (source from selected) | Notes | yes |
| `graph.next-view` | Switch to the next view | Views | no |
| `graph.open-in-editor` | Open the selected note in $EDITOR | Notes | no |
| `graph.open-in-obsidian` | Open the selected note in Obsidian | Notes | no |
| `graph.periodic-leader` | Enter the periodic-note leader (then d/w/m/q/y) | Periodic notes | yes |
| `graph.preset-pick` | Load a preset into the active view | Views | yes |
| `graph.prev-view` | Switch to the previous view | Views | no |
| `graph.query-bar` | Open the query bar to edit the active view's query | Query | yes |
| `graph.quick-capture` | Quick capture (run a preset) | Notes | yes |
| `graph.refresh` | Refresh the graph from disk | Notes | no |
| `graph.related` | Open the Related-section updater modal for the selected note | Notes | yes |
| `graph.rename-or-multi-move` | Rename the selected node (or move multi-selection) | Notes | yes |
| `graph.rewrite-for-root` | Re-root the active view's query on the selected node | Query | no |
| `graph.search` | Open the in-tree fuzzy search picker | Query | yes |
| `graph.switch-view` | Switch to the view at the given 0-based index | Views | no |
| `graph.today` | Open today's daily note | Periodic notes | no |
| `graph.toggle-multi-select` | Toggle multi-selection on the focused row | Notes | no |

## tab/journal

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `journal.clear` | Clear the current journal and return to the picker prompt | Source | no |
| `journal.cursor-down` | Move the cursor down one entry | Navigation | no |
| `journal.cursor-first` | Move the cursor to the first entry | Navigation | no |
| `journal.cursor-half-page-down` | Move the cursor down half a page (10 entries) | Navigation | no |
| `journal.cursor-half-page-up` | Move the cursor up half a page (10 entries) | Navigation | no |
| `journal.cursor-last` | Move the cursor to the last entry | Navigation | no |
| `journal.cursor-up` | Move the cursor up one entry | Navigation | no |
| `journal.open-picker` | Open the fuzzy note picker to choose a journal source | Source | yes |
| `journal.open-selected` | Open the selected entry's note in $EDITOR | Open | no |
| `journal.reload` | Reload the current note's journal | Source | no |

## tab/notes

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `notes.append` | Append a template to a note | Notes | yes |
| `notes.create-blank` | Create a new note (blank) | Notes | yes |
| `notes.create-from-template` | Create a new note from a template | Notes | yes |
| `notes.move-section` | Enter the move-section flow | Notes | yes |
| `notes.open-picker` | Open the fuzzy file / heading picker to open a note | Notes | yes |
| `notes.periodic-leader` | Enter the periodic-note leader (then d/w/m/q/y) | Periodic notes | yes |
| `notes.quick-capture` | Quick capture (run a preset) | Notes | yes |
| `notes.today` | Open today's daily note | Periodic notes | no |

## tab/tasks

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `tasks.cancel` | Cancel the selected task | Mutations | no |
| `tasks.complete` | Complete the selected task | Mutations | no |
| `tasks.confirm-view` | Confirm the sidebar's selected view (no-op for now) | Sidebar | no |
| `tasks.cursor-down` | Move the cursor down one task | Navigation | no |
| `tasks.cursor-up` | Move the cursor up one task | Navigation | no |
| `tasks.due-next-day` | Bump the due date forward by one day | Mutations | no |
| `tasks.due-prev-day` | Bump the due date back by one day | Mutations | no |
| `tasks.due-today` | Set due date to today | Mutations | no |
| `tasks.edit-popup` | Open the edit-popup for the selected task | Create / edit | yes |
| `tasks.edit-query` | Open the query editor | Navigation | yes |
| `tasks.new-blank-form` | Open the new-task form (blank) | Create / edit | yes |
| `tasks.open-in-editor` | Open the selected task in $EDITOR | Navigation | no |
| `tasks.priority-next` | Cycle priority forward | Mutations | no |
| `tasks.priority-prev` | Cycle priority back | Mutations | no |
| `tasks.quickline` | Open the quickline new-task entry | Create / edit | yes |
| `tasks.reload` | Reload the task list from the vault | Navigation | no |
| `tasks.scheduled-next-day` | Bump the scheduled date forward by one day | Mutations | no |
| `tasks.scheduled-prev-day` | Bump the scheduled date back by one day | Mutations | no |
| `tasks.select-next-view` | Select the next view in the sidebar | Sidebar | no |
| `tasks.select-prev-view` | Select the previous view in the sidebar | Sidebar | no |

## tab/timeblocks

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `timeblocks.add-form` | Add a new block via the multi-field form | Create / edit / delete | yes |
| `timeblocks.add-quickline` | Add a new block via quickline entry | Create / edit / delete | yes |
| `timeblocks.anchor-back` | Slide the anchor day back by one day | Navigation | no |
| `timeblocks.anchor-forward` | Slide the anchor day forward by one day | Navigation | no |
| `timeblocks.anchor-today` | Jump the anchor day to today | Navigation | no |
| `timeblocks.block-earlier` | Shift the focused block -5 minutes (duration preserved) | Edit times | no |
| `timeblocks.block-later` | Shift the focused block ±5 minutes (duration preserved) | Edit times | no |
| `timeblocks.create-daily` | Create the missing daily note | Create / edit / delete | no |
| `timeblocks.cursor-down` | Move the cursor down one block | Navigation | no |
| `timeblocks.cursor-first` | Jump to the first block | Navigation | no |
| `timeblocks.cursor-last` | Jump to the last block | Navigation | no |
| `timeblocks.cursor-up` | Move the cursor up one block | Navigation | no |
| `timeblocks.delete-start` | Arm the two-stroke `d d` delete chord | Create / edit / delete | no |
| `timeblocks.edit-desc` | Edit the focused block's description inline | Create / edit / delete | yes |
| `timeblocks.end-earlier` | Shift the focused block's end -5 minutes | Edit times | no |
| `timeblocks.end-later` | Shift the focused block's end +5 minutes | Edit times | no |
| `timeblocks.reload` | Reload the visible panes from disk | Navigation | no |
| `timeblocks.start-earlier` | Shift the focused block's start -5 minutes | Edit times | no |
| `timeblocks.start-later` | Shift the focused block's start +5 minutes | Edit times | no |
| `timeblocks.start-tagging` | Add or remove @tags on the focused block | Create / edit / delete | yes |
| `timeblocks.toggle-pane-back` | Toggle focus to the previous pane (Split) / previous day (Single) | Navigation | no |
| `timeblocks.toggle-pane-forward` | Toggle focus to the next pane (Split) / next day (Single) | Navigation | no |
| `timeblocks.toggle-view` | Toggle between Split and Single view | Navigation | no |

## modal/append

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `append.cancel` | Cancel / step back | Flow | no |
| `append.confirm` | Confirm the current step | Flow | no |
| `append.cursor-down` | Select the next candidate | Navigation | no |
| `append.cursor-up` | Select the previous candidate | Navigation | no |

## modal/capture-picker

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `capture-picker.cancel` | Cancel / step back | Flow | no |
| `capture-picker.confirm` | Confirm the current step | Flow | no |
| `capture-picker.cursor-down` | Select the next capture preset | Navigation | no |
| `capture-picker.cursor-up` | Select the previous capture preset | Navigation | no |

## modal/capture-var

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `capture-var.cancel` | Cancel / step back | Flow | no |
| `capture-var.next-or-commit` | Advance to the next var (or commit the capture) | Flow | no |

## modal/create

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `create.cancel` | Cancel / step back | Flow | no |
| `create.confirm` | Confirm the current step | Flow | no |
| `create.cursor-down` | Select the next candidate | Navigation | no |
| `create.cursor-up` | Select the previous candidate | Navigation | no |

## modal/move

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `move.cancel` | Cancel / step back | Flow | no |
| `move.pick-from-list` | Open the fuzzy picker for source/target selection | Flow | yes |
| `move.use-selected-as-source` | Confirm the tree's selected note as the move source | Flow | no |
| `move.use-selected-as-target` | Confirm the tree's selected note as the move target | Flow | no |

## modal/periodic-leader

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `periodic-leader.cancel` | Cancel / step back | Flow | no |
| `periodic-leader.daily` | Open today's daily note | Periodic | no |
| `periodic-leader.monthly` | Open this month's monthly note | Periodic | no |
| `periodic-leader.quarterly` | Open this quarter's quarterly note | Periodic | no |
| `periodic-leader.weekly` | Open this week's weekly note | Periodic | no |
| `periodic-leader.yearly` | Open this year's yearly note | Periodic | no |

## modal/preset-picker

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `preset-picker.cancel` | Cancel / step back | Flow | no |
| `preset-picker.confirm` | Confirm the current step | Flow | no |
| `preset-picker.cursor-down` | Select the next preset | Navigation | no |
| `preset-picker.cursor-up` | Select the previous preset | Navigation | no |

## modal/query-bar

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `query-bar.apply` | Apply the edited query | Flow | no |
| `query-bar.cancel` | Cancel / step back | Flow | no |

## modal/related

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `related.cancel` | Cancel / step back | Flow | no |
| `related.commit` | Append the checked concepts to the note's Related section | Flow | no |
| `related.cursor-down` | Focus the next candidate | Navigation | no |
| `related.cursor-up` | Focus the previous candidate | Navigation | no |
| `related.toggle` | Toggle the focused candidate's selection | Flow | no |

## modal/rename

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `rename.cancel` | Cancel / step back | Flow | no |
| `rename.commit` | Commit the rename | Flow | no |

## modal/search

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `search.cancel` | Cancel / step back | Flow | no |
| `search.confirm` | Confirm the current step | Flow | no |
| `search.cursor-down` | Select the next candidate | Navigation | no |
| `search.cursor-up` | Select the previous candidate | Navigation | no |

## modal/section-move

| Name | Description | Group | Opens modal |
| --- | --- | --- | --- |
| `section-move.cancel` | Cancel / step back | Flow | no |
| `section-move.confirm` | Confirm the current step | Flow | no |
| `section-move.cursor-down` | Focus the next heading | Navigation | no |
| `section-move.cursor-up` | Focus the previous heading | Navigation | no |
| `section-move.toggle` | Toggle the focused heading's selection | Flow | no |

